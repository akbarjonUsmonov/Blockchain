#ifndef BLOCKCHAIN_H_CONVERTOR_H_
#define BLOCKCHAIN_H_CONVERTOR_H_

#include "./my_string.h"

int my_atoi(char*);
int my_isdigit(char);
int number_check(char*);
char* my_rev(char*);
char* my_itoa(int, char*);

#endif